## 1. 升级资产模型与版本元数据

- [x] 1.1 定义统一的升级计划对象与资产模型，至少覆盖 `package_manifest`、`connector_manifest`、`config_merge_plan`、`node_targets`。
- [x] 1.2 建立统一的版本元数据入口，集中管理推荐版本、默认版本与版本列表来源。
- [x] 1.3 替换 frontend / backend / agent 中分散的 `2.3.12` 默认值调用点，改为统一读取版本元数据入口。

## 2. 升级领域建模与持久化

- [x] 2.1 创建新的控制面升级模块结构，并定义预检查、计划、执行、回滚、日志流所需的共享步骤编码与状态常量。
- [x] 2.2 为升级任务、任务步骤、节点执行、计划快照与步骤日志记录新增持久化模型和迁移。
- [x] 2.3 打通 repository / service，使升级任务状态能够在后端重启后继续查询，且不依赖当前内存态 task manager。

## 3. 预检查与计划生成

- [x] 3.1 实现集群升级预检查，校验安装包本地存在性、checksum 完整性、架构匹配、连接器可用性、节点就绪状态以及配置合并前提。
- [x] 3.2 将 checksum 校验升级为硬门禁：缺少 checksum、checksum 不匹配或无法验证时，必须阻断计划生成与执行。
- [x] 3.3 实现升级计划生成，落盘 source / target 版本、目标节点、安装包来源、连接器清单、配置合并摘要与有序执行步骤。
- [x] 3.4 新增升级预检查与计划创建 / 查询后端 API，并为校验失败场景返回阻断型问题列表。

## 4. 执行与回滚编排

- [x] 4.1 在 Agent 侧增加升级原语：备份、目标版本解压、基于 manifest 的 connector / lib 同步、配置应用、切换、恢复与校验，且不再走遗留的卸载式升级路径。
- [x] 4.2 在控制面升级编排链路中禁止复用 legacy Agent `upgrade` handler，并确保 UI 不直接暴露该危险路径。
- [x] 4.3 实现控制面的批次升级编排器，按固定步骤编码顺序执行，并协调节点停启与节点级进度记录。
- [x] 4.4 实现自动回滚：当任一备份后的执行步骤失败时，恢复旧版本安装 / 配置 / 连接器状态，并同时记录原始失败原因与回滚结果。

## 5. 升级可视化 API 与事件流

- [x] 5.1 新增任务详情、步骤详情与可过滤日志查询 API，并以持久化升级任务数据作为后端来源。
- [x] 5.2 新增升级执行与回滚进度事件流推送，使前端能够近实时刷新当前任务状态。
- [x] 5.3 确保升级执行为每个关键步骤转换写入可读消息、命令摘要与节点级日志记录。

## 6. 前端三页流程

- [x] 6.1 新增升级准备页，展示目标版本选择、预检查结果、缺失资产以及升级计划摘要，并提供一键补齐引导。
- [x] 6.2 新增配置合并页，展示 `base / local / target` 三方配置、冲突处理与最终合并结果预览。
- [x] 6.3 新增升级执行页，展示阶段条、步骤树、节点状态、回滚状态与实时日志，而不是单一 wizard 弹窗。

## 7. 验证与兼容性收口

- [x] 7.1 明确本次 MVP 提交不包含 hybrid / separated 两类拓扑的全量自动化与手工验证，该项覆盖延后到后续迭代。
- [x] 7.2 验证 checksum 硬门禁、legacy upgrade 禁用、双目录切换与自动回滚在失败场景下的行为正确性。
- [x] 7.3 验证统一版本元数据入口已覆盖前端、后端与 Agent 默认值来源，避免继续新增散落硬编码。
